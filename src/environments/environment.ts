export const environment = {
  production: false,
  useStubs: true, // true = load from /json/stub-data.json instead of API
  apiBase: 'http://localhost:3000' // ajusta si tu server corre en otro host/puerto
  //apiBase: 'https://emefjs.duckdns.org'
};

